# Tracelane — Marketing Site Content (Source of Truth)

Marketing site only (`tracelane/site`, Astro). Copy here is the canonical source for
the public homepage. Use VERBATIM — do not rewrite, expand, or "improve".

Honesty-lock: say "tamper-evident" (never "tamper-proof"). Observe verbs only
(record / surface / reconstruct / see / prove) — no prevention claims. No launch date.

---

## Hero

**H1:** The flight recorder for AI agents

**Subhead:** Autonomous agents fail without crashing. Every call returns a clean 200, every token is well-formed — and the run is already three steps off course. Tracelane records the entire execution path to a tamper-evident, cryptographically signed ledger. See exactly where it drifted. Prove exactly what happened.

**CTA:** unchanged (coming soon / waitlist).

---

## Section — "Why agent failures stay invisible" (3 cards)

**Card 1:** State drift compounds. A bad tool output at step 2 corrupts every step after it. The model keeps going, confidently, further from the goal.

**Card 2:** Success codes lie. A 200 and clean tokens say nothing about whether the state is still valid three steps later. Tracelane reconstructs the causal path, not just the request log.

**Card 3:** Tools are an open execution surface. Every tool call is code running on your behalf. The ledger is the cryptographic record of exactly what ran.
